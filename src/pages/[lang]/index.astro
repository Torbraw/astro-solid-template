---
import BaseLayout from '@/layouts/base-layout.astro';
import { getLangFromUrl } from '@/lib/utils';

const lang = getLangFromUrl(Astro.url);
const user = Astro.locals.user;
---

<BaseLayout title="Index" lang={lang} user={user} />
